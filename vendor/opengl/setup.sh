#!/usr/bin/env bash

# This script is used to set up the environment for building libopengl
# on the host machine.
sudo apt update

# Install potential dependencies
sudo apt install -y mesa-utils libglu1-mesa-dev freeglut3-dev mesa-common-dev


