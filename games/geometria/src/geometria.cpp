#include "geometria.h"

using namespace bz::engine;
using namespace bz::engine::graphics;

void Geometria::startup(Engine *, Window *window, Scene *scene,
                        Renderer *renderer) {
	(void)window;
	(void)scene;
	(void)renderer;
}

void Geometria::input(Window *window, Scene *scene, bool inputHandled) {
	(void)window;
	(void)scene;
	(void)inputHandled;
}